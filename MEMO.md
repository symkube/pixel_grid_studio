# 产品备忘

## 后续设计

- 参数设置 UI 需要整体重新设计，等待用户逐步提供方案；当前保留现有参数控件。
- 待设计内容：参数分组、默认展开层级、候选间距展示、阈值反馈、桌面与移动端布局。
- 修改颜色的操作逻辑已按当前方案重做：查看原图/修改后与分类强调分离，分类查看对非目标格叠加细线纹理，单格取色方式放到中央工具栏；后续仍可继续细化按钮分组与颜色编辑流程。

## 当前流程约定

1. 选择图片：导入后可裁剪、跳过或取消；裁剪完成后才进入识别步骤。
2. 自动识别：用户主动执行，允许返回修改参数后重新识别。
3. 手动调整网格：只允许连续、同方向的内部边界线选择；合并坐标取算术平均值；不修改原图。
4. 手动调整颜色：阈值聚类众数、均值取色、手动指定。手动颜色优先，在中央切换所选格子的取色方式可清除其覆盖。第四步已移除矩形取样面板、移动取样及其快捷键，旧项目数据保留兼容。

宽窄异常分别提供批量处理与逐项确认（确认、跳过、结束）。图像外边界固定；过窄边缘格通过删除内部边界合并。批量操作只处理当前异常，使用局部间距和梯度，不重新识别整个网格。操作支持撤销。

第 2、3、4 步底部提供独立的参数重置。识别步骤恢复默认识别配置；网格步骤恢复校准、吸附和显示配置（行列与边界仍反映当前网格）；颜色步骤恢复取色参数与显示配置。参数重置不清空网格和手动颜色，清除颜色编辑由单独的「重置回初始状态」完成。

手动取色采用一次性吸管：自动定位放大当前格子，原图内点击完成即退出，支持 Esc 取消；独立吸管工具入口移除。均值取色不计完全透明像素，导入有效透明度图片时提供保留、铺底色、可见像素转实色选项。
